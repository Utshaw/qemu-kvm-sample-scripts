# Install 
`install_virt_stack.sh`

# Delete if already utshaw_vm exists (everything)
`cleanup_utshaw_vm.sh`

# Create the disk
`create_disk.sh`

# Create the VM
`create_vm.sh`

# Start the VM and view
`start_vm_and_view.sh`
